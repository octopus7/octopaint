<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="OctoPaint.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="OctoPaint">

    <Grid x:Name="RootGrid" Loaded="RootGrid_Loaded" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Icon="Add" Label="New document" Click="NewDocument_Click" />
        </CommandBar>

        <Grid Grid.Row="1" ColumnSpacing="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="260" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Tools" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="Frontend-only placeholder" Opacity="0.65" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Margin="12" Background="#202124" CornerRadius="6">
                <Grid>
                    <TextBlock
                        x:Name="DocumentTitleText"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Foreground="White"
                        FontSize="24" />
                </Grid>
            </Border>

            <Border Grid.Column="2" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Layers" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="Application state will be projected here." Opacity="0.65" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Padding="12,6" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
            <TextBlock x:Name="StatusText" />
        </Border>
    </Grid>
</Window>

