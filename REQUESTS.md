# 요청 시작: 2026-08-05 23:57:39 KST | 작업 완료: 2026-08-05 23:58:09 KST (소요 시간: 30초)

## 구현 내역

- 저장소 루트에 영구 지침 파일 `AGENTS.md`를 추가했다.
- 모든 구현 및 변경 요청을 영어 파일명 `REQUEST.md`에 누적 기록하도록 규정했다.
- 각 기록에 요청 시작 시각, 작업 완료 시각, 소요 시간을 제목으로 작성하고 그 아래에 구현 내역과 결정 내용을 남기는 형식을 명시했다.

## 결정 내용

- 요청 기록을 쉽게 찾고 이전 이력을 보존할 수 있도록 단일 파일 `REQUEST.md`에 요청별 항목을 누적한다.
- 시간대는 저장소 작업 환경에 맞춰 KST로 명시한다.
- 소요 시간은 요청 시작 시각부터 작업 완료 시각까지의 실제 경과 시간으로 계산한다.

---

# 요청 시작: 2026-08-05 23:58:44 KST | 작업 완료: 2026-08-05 23:59:05 KST (소요 시간: 21초)

## 구현 내역

- 간단한 질문과 답변을 `QUESTION.md`에 누적 기록하는 영구 지침을 추가했다.
- 질문 제목 옆에는 질문 시각만 `HH:mm:ss KST` 형식으로 기록하도록 규정했다.
- 질문 아래에 원문 질문과 답변을 함께 기록하는 형식을 명시했다.

## 결정 내용

- 구현이나 저장소 변경을 요구하지 않는 간단한 질의만 질문 기록으로 분류한다.
- 간단한 질문에는 작업 완료 시각과 소요 시간을 기록하지 않는다.
- 파일명 영어 사용 원칙에 따라 질문 기록 파일명을 `QUESTION.md`로 정한다.

---

# 요청 시작: 2026-08-05 23:59:45 KST | 작업 완료: 2026-08-06 00:00:10 KST (소요 시간: 25초)

## 구현 내역

- 누적 기록 파일명을 `REQUEST.md`에서 `REQUESTS.md`로 변경했다.
- 누적 기록 파일명을 `QUESTION.md`에서 `QUESTIONS.md`로 변경했다.
- `AGENTS.md`의 모든 활성 파일 참조를 복수형 파일명으로 수정했다.
- 이번 질문과 답변을 `QUESTIONS.md`에 기록했다.

## 결정 내용

- 각 파일이 여러 요청 또는 질문 항목을 누적해서 담으므로 복수형 파일명이 의미상 더 정확하다.
- 과거 요청 기록은 당시 결정 이력을 보존하기 위해 내용 중 단수형 파일명 언급을 소급 수정하지 않는다.

---

# 요청 시작: 2026-08-06 00:21:40 KST | 작업 완료: 2026-08-06 00:22:14 KST (소요 시간: 34초)

## 구현 내역

- 구현 요청과 작업 중 추가 지시에서 분리 가능한 작업을 병렬 에이전트에 할당하는 규칙을 영구 지침에 추가했다.
- 논리적으로 종결된 서로 다른 작업을 독립적인 커밋으로 작성하는 규칙을 영구 지침에 추가했다.
- 병렬 작업의 파일 소유권 분리, 주 에이전트의 통합·검증 책임, 요청 기록과 커밋의 결합 원칙을 명시했다.

## 결정 내용

- 병렬화는 독립적으로 수행할 수 있고 파일 또는 서브시스템 소유권을 분리할 수 있을 때 적용한다.
- 커밋은 단순히 파일을 나누는 단위가 아니라 검증 가능한 하나의 완료 결과를 나타낸다.
- 추가 지시가 기존 작업과 논리적으로 다른 경우 별도 작업과 별도 커밋으로 처리한다.

---

# 요청 시작: 2026-08-06 00:23:33 KST | 작업 완료: 2026-08-06 00:23:53 KST (소요 시간: 20초)

## 구현 내역

- Visual Studio 사용자 상태와 데이터베이스 파일을 제외하는 `.gitignore` 규칙을 추가했다.
- C++ 컴파일 중간 파일과 프로젝트 출력 디렉터리를 제외하도록 규칙을 확장했다.
- NuGet 패키지, WinUI/MSIX 패키징 산출물, 테스트 및 프로파일링 결과를 제외하도록 구성했다.

## 결정 내용

- 소스, 프로젝트 구성, 패키지 잠금 파일은 추적 대상에 유지한다.
- 재생성 가능한 로컬 빌드·패키징·IDE 산출물만 제외한다.
- 광범위한 `Debug` 또는 `Release` 디렉터리 패턴 대신 현재 통합 출력 경로인 `out/`을 제외해 같은 이름의 소스 디렉터리가 실수로 무시되지 않게 한다.

---

# 요청 시작: 2026-08-06 00:20:41 KST | 작업 완료: 2026-08-06 00:28:20 KST (소요 시간: 7분 39초)

## 구현 내역

- 다중 문서, 레이어 트리, 블렌딩 모드, 레이어 마스크, 채널과 선택 영역의 도메인 구조를 설계했다.
- 명도·대비, 채도, 색조, 커브, Desaturate 조정과 Gaussian Blur를 포함한 확장형 이미지 연산자 계약을 정의했다.
- Crop, 9방향 기준 Canvas Resize, 비율 또는 픽셀 기반 Image Resample 명령을 구분해 설계했다.
- 256×256 희소 Copy-on-Write 타일 엔진, 문서별 Undo/Redo, 불변 렌더 스냅샷, 비동기 revision 검증 구조를 문서화했다.
- `.ocp` ZIP64/CBOR 컨테이너, 무결성 검증, 원자 저장, PNG/JPEG 교환, PSD 읽기·쓰기 호환성 및 손실 보고 정책을 설계했다.
- 제품 요구사항, 편집기 아키텍처, 파일 포맷 설계를 각각 문서로 작성했다.

## 결정 내용

- WinUI 3 실행 프로젝트는 `OctoPaint`라는 제품 표기만 노출하며, UI는 Application API를 사용하는 교체 가능한 어댑터로 제한한다.
- Core는 표준 C++23만 사용하고 Direct3D, 코덱, WinUI 구현은 포트 뒤의 별도 어댑터로 둔다.
- 기본 렌더 경로는 D3D11/HLSL이며 모든 필수 연산에 CPU 참조 경로를 둔다.
- `.ocp`를 무손실 편집 기준 포맷으로 삼고 PNG/JPEG는 평면 내보내기, PSD는 호환성 진단이 동반되는 교환 포맷으로 취급한다.
- PSD에서 표현할 수 없는 기능은 조용히 손실시키지 않고 래스터화·병합 항목을 저장 전에 보고한다.

---

# 요청 시작: 2026-08-06 00:27:24 KST | 작업 완료: 2026-08-06 00:29:30 KST (소요 시간: 2분 6초)

## 구현 내역

- 기본 영어 문서 `README.md`를 작성했다.
- 한국어 `README_KO.md`와 일본어 `README_JA.md`를 추가했다.
- 각 README 최상단에 현재 언어를 제외한 나머지 두 언어 문서 링크를 배치했다.
- 세 README에 제품 범위, 아키텍처, 저장소 구조, 설계 문서, 빌드와 테스트 정보를 같은 구조로 기록했다.
- README 다국어 유지 규칙을 영구 지침에 추가했다.

## 결정 내용

- 접미사가 없는 `README.md`를 기본 영어 문서로 사용한다.
- 번역 문서는 대문자 2자 언어 접미사를 사용해 `README_KO.md`, `README_JA.md`로 명명한다.
- 각 문서는 자기 자신으로 연결하지 않고 다른 언어 문서만 상단에 표시한다.
- README 내용이 변경되면 세 언어 문서의 의미와 구조를 함께 갱신한다.

---

# 요청 시작: 2026-08-06 00:13:22 KST | 작업 완료: 2026-08-06 00:30:16 KST (소요 시간: 16분 54초)

## 구현 내역

- Visual Studio 2022용 C++23 솔루션 `OctoPaint.sln`을 구성했다.
- 표준 C++ 전용 `OctoPaint.Core`, UI 중립 `OctoPaint.Application`, 교체 가능한 WinUI 3 실행 어댑터를 분리했다.
- Application 공개 API에 WinUI, WinRT, Win32와 Direct3D 타입이 노출되지 않도록 PImpl 기반 경계를 구현했다.
- WinUI 3 최소 창, 도구·레이어 패널 자리, 문서 생성 명령과 상태 스냅샷 투영을 구현했다.
- 제품 프로젝트 파일을 `OctoPaint.vcxproj`, 솔루션 표시명을 `OctoPaint`, 실행 파일을 `OctoPaint.exe`, 창 제목을 `OctoPaint`로 통일했다.
- UI 없이 Core/Application을 검증하는 `OctoPaint.Core.Tests` 실행 테스트를 추가했다.
- 프런트엔드 교체 규칙과 의존성 방향을 `docs/ARCHITECTURE.md`에 문서화했다.

## 결정 내용

- 프런트엔드는 Application 명령과 값 스냅샷만 사용하며 문서 모델을 직접 변경하지 않는다.
- Core는 C++23 표준 라이브러리만 사용하고 플랫폼별 UI와 렌더링은 바깥쪽 어댑터로 둔다.
- 설치된 MSVC 14.38이 `/std:c++23preview`를 인식하지 않으므로, C++23 기능을 사용할 수 있는 `/std:c++latest` 모드로 빌드한다.
- WinUI 3 앱은 현재 unpackaged 구성을 사용하며 Windows App SDK 의존성은 Native NuGet 참조로 복원한다.
- 내부 라이브러리와 namespace는 책임을 구분하기 위해 한정 이름을 유지하되 사용자에게 노출되는 앱 이름은 모두 `OctoPaint`로 통일한다.

---

# 요청 시작: 2026-08-06 00:32:37 KST | 작업 완료: 2026-08-06 00:33:00 KST (소요 시간: 23초)

## 구현 내역

- 모든 성공한 커밋을 생성 직후 구성된 원격 저장소로 푸시하는 규칙을 영구 지침에 추가했다.
- 푸시 실패 시 작업을 완전히 전달된 것으로 처리하지 않고 오류를 보고하고 해결하도록 규정했다.

## 결정 내용

- 커밋과 원격 푸시를 하나의 완료 흐름으로 취급한다.
- 서로 다른 작업은 각각 독립 커밋한 뒤 해당 커밋을 즉시 푸시한다.

---

# 요청 시작: 2026-08-06 00:36:39 KST | 작업 완료: 2026-08-06 00:37:33 KST (소요 시간: 54초)

## 구현 내역

- Pencil, Airbrush, 사각·타원 Marquee, Freehand·Polygonal Lasso, Move Layer 도구 요구사항을 제품 문서에 추가했다.
- Pencil의 정수 픽셀 양자화와 무안티앨리어싱 연결선 규칙, Airbrush의 시간·압력 기반 누적 규칙을 설계했다.
- 도구 gesture, preview overlay와 단일 undo 명령의 Application 계약을 정의했다.
- 왼쪽 세로 툴바와 하단의 대각선 중첩 전경·배경색 스와치, 색 교환·초기화 동작을 설계했다.
- 스와치별 HSV 피커와 Hue/Saturation/Value/Alpha, RGB 및 hex 입력 동기화 요구사항을 문서화했다.

## 결정 내용

- 활성 도구, 도구 설정과 전경·배경색은 WinUI 내부가 아니라 교체 가능한 Application 상태로 관리한다.
- 프런트엔드는 플랫폼 입력을 중립 `PointerSample`로 변환하고 도구 상태 기계의 결과만 투영한다.
- Pencil은 coverage 0 또는 1의 정수 픽셀만 기록하고 빠른 이동 구간은 정수 선 알고리즘으로 연결한다.
- 레이어 자체 이동과 선택 픽셀 이동을 서로 다른 명령으로 유지한다.
- HSV 피커의 미리보기는 임시 상태이며 확정 시 하나의 색 변경 명령으로 기록한다.

---

# 요청 시작: 2026-08-06 00:34:30 KST | 작업 완료: 2026-08-06 00:43:14 KST (소요 시간: 8분 44초)

## 구현 내역

- 재사용되지 않는 강타입 `DocumentId`와 문서 생성, 닫기, 활성화, 조회 API를 구현했다.
- 여러 문서를 동시에 유지하고 활성 문서를 전환할 수 있는 `Workspace` 상태와 분리된 값 스냅샷을 구현했다.
- 문서마다 독립적인 명령 실행, 실행 취소, 다시 실행 기록과 명령 라벨을 구현했다.
- 저장 revision과 현재 revision을 비교해 문서별 변경 상태를 판정하도록 구현했다.
- 활성 문서 편의 API와 기존 단일 문서 스냅샷 필드를 유지해 기존 WinUI 호출부의 소스 호환성을 보존했다.
- 전용 Application 테스트 프로젝트를 솔루션에 추가하고 문서 전환, 기록 격리, 분기 제거, 저장 상태 복귀, 닫힌 ID 거부를 검증했다.

## 결정 내용

- 닫힌 문서 ID는 프로세스 수명 동안 재사용하지 않아 오래된 UI 상태가 새 문서를 가리키지 않게 한다.
- 편집 동작은 `DocumentCommand`와 제한된 `DocumentMutation` 경계를 통해서만 문서를 변경한다.
- 각 문서는 독립적인 실행 취소 커서와 저장 revision을 가지며 새 명령이 실행되면 해당 문서의 redo 분기만 제거한다.
- UI에는 내부 객체 참조가 아니라 복사된 `WorkspaceSnapshot`만 제공한다.

---

# 요청 시작: 2026-08-06 00:34:30 KST | 작업 완료: 2026-08-06 00:44:46 KST (소요 시간: 10분 16초)

## 구현 내역

- 고정 256×256 premultiplied RGBA8 타일과 결정적인 좌표 키를 구현했다.
- 비어 있는 타일을 할당하지 않는 희소 타일 저장소와 완전 투명 타일 자동 제거를 구현했다.
- 저장소 복사 시 불변 타일 payload를 공유하고 편집한 타일만 복제하는 타일 단위 Copy-on-Write를 구현했다.
- 안정적인 `LayerId`, 공통 레이어 속성, Raster/Group 레이어 및 블렌딩 모드 계약을 구현했다.
- 루트와 그룹 자식 순서를 보존하는 중첩 `LayerTree`와 추가, 제거, 이동, 조회 API를 구현했다.
- 중복 ID, 잘못된 부모, 범위 오류, 자기 자신 및 하위 그룹으로의 순환 이동을 결정적인 오류 코드로 거부하도록 구현했다.
- 전용 Core 도메인 테스트 프로젝트를 솔루션에 연결해 희소 저장, COW, 레이어 속성, 중첩과 이동 규칙을 검증했다.

## 결정 내용

- Core 도메인 공개 API에는 WinUI, WinRT, Win32 및 Direct3D 형식을 노출하지 않는다.
- 타일 저장 순회 순서는 `std::map` 기반 `TileKey` 순서로 결정성을 보장한다.
- 타일 payload는 발행 후 불변으로 취급하고 `BeginWrite`로 얻은 초안만 변경할 수 있게 한다.
- 레이어 ID는 파일 저장과 외부 참조에 안정적으로 사용할 수 있도록 호출자가 부여하는 0이 아닌 값으로 정의한다.
- 이동 인덱스는 원본 레이어를 제거한 뒤의 최종 목적지 순서를 기준으로 해석한다.

---

# 요청 시작: 2026-08-06 00:29:01 KST | 작업 완료: 2026-08-06 00:46:51 KST (소요 시간: 17분 50초)

## 구현 내역

- 저장소 루트의 `VERSION`을 릴리스 버전 단일 소스로 추가했다.
- Visual Studio 의존성 복원, 자체 포함 Release x64 빌드, headless 테스트 실행, 런타임 스테이징을 수행하는 `build-release.bat`을 구현했다.
- ZIP 파일을 `OctoPaint-<version>-win-x64.zip` 이름으로 생성하도록 구현하고 `0.1.0` 산출물의 실행 파일 포함 및 개발 산출물 제외를 검증했다.
- WiX Toolset 5 이상으로 x64 per-machine MSI를 `OctoPaint-<version>-win-x64.msi` 이름으로 생성하도록 구현했다.
- MSI에 내장 CAB, MajorUpgrade, Program Files 설치, 시작 메뉴 바로가기와 제거 시 정리 규칙을 추가했다.
- 영문, 한국어, 일본어 README에 동일한 릴리스 명령, 준비 사항과 버전 포함 ZIP/MSI 파일명을 기록했다.
- 생성된 MSI와 WiX 중간 산출물을 Git 추적 대상에서 제외했다.

## 결정 내용

- 설치 배포 형식은 별도 setup EXE 없이 MSI만 제공한다.
- ZIP과 MSI는 모두 `VERSION`의 `major.minor.patch` 값을 파일명에 반드시 포함한다.
- WiX가 없거나 5 미만이면 자동 설치하지 않고 공식 설치 명령을 포함한 명확한 오류로 빌드 시작 전에 종료한다.
- 현재 개발 환경에는 WiX가 없어 MSI 컴파일 대신 WiX XML 구문과 누락 도구 fail-fast를 검증했으며, WiX가 있는 릴리스 환경에서 같은 배치가 MSI를 생성한다.

---

# 요청 시작: 2026-08-06 00:52:15 KST | 작업 완료: 2026-08-06 00:53:07 KST (소요 시간: 52초)

## 구현 내역

- 저장소 전체 구현 상태를 체크리스트로 추적하는 `PROGRESS.md`를 추가했다.
- 프로젝트 기반, 문서와 기록, 타일과 레이어, 도구, WinUI, 렌더링과 이미지 연산, 파일 호환, 빌드와 검증 영역으로 작업을 분류했다.
- 완료, 진행 중, 미착수 상태를 구분하고 현재 병렬 구현 중인 항목을 표시했다.
- 현재 전달 슬라이스의 통합, 전체 빌드와 테스트, 독립 커밋 및 즉시 푸시 항목을 명시했다.

## 결정 내용

- 진행 현황 문서 파일명은 영어 `PROGRESS.md`를 사용한다.
- 체크된 항목은 검증을 마친 기능만 의미하며, 구현 중인 항목은 체크하지 않고 `🚧` 표시를 함께 사용한다.
- 이후 구현이 완료되거나 범위가 추가될 때 이 체크리스트를 함께 갱신한다.

---

# 요청 시작: 2026-08-06 00:53:38 KST | 작업 완료: 2026-08-06 00:59:17 KST (소요 시간: 5분 39초)

## 구현 내역

- 현재 확정 계획에 포함되지 않은 그래픽 편집기 기능을 검토하는 `CANDIDATE_FEATURES.md`를 추가했다.
- 기본 작업 흐름, 페인팅, 선택과 변형, 레이어, 보기와 탐색, 색 관리, 안정성, 입력과 접근성, 상호 운용 범주로 후보를 분류했다.
- 각 후보에 필요한 이유, 의존성·예상 비용, 채택·보류·제외 검토란을 기록했다.
- 기존 확정 범위를 별도로 명시해 후보와 구현 대상이 섞이지 않게 했다.
- 이후 확정된 브러시 세부 제어, 스타일러스 감도 및 Stroke Stabilizer는 후보에서 제거하고 확정 범위에 포함했다.

## 결정 내용

- 후보 문서는 구현 승인 문서가 아니며, 문서에 있다는 이유로 자동 구현하지 않는다.
- 사용자가 후보를 명시적으로 채택한 경우에만 제품 요구사항과 아키텍처 반영, `PROGRESS.md` 등록, 별도 구현 요청 순서로 승격한다.
- 레이어 투명도 잠금처럼 이미 확정된 요구는 후보 결정 대상에 포함하지 않는다.

---

# 요청 시작: 2026-08-06 00:59:02 KST | 작업 완료: 2026-08-06 01:00:32 KST (소요 시간: 1분 30초)

## 구현 내역

- `PROGRESS.md`의 제목, 설명, 섹션과 체크리스트 항목을 한국어로 변경했다.
- 기존 완료·진행·대기 상태와 체크박스 의미를 그대로 유지했다.
- 최신 확정 요구인 브러시 세부 옵션, 압력 매핑 토글, Stroke Stabilizer, Tool Options Bar와 스타일러스 감도 설정 항목을 진행 중 상태로 추가했다.
- 후보 기능 검토 문서 작성 완료 상태를 체크리스트에 반영했다.

## 결정 내용

- 진행 현황 파일명은 영어 `PROGRESS.md`를 유지하고 본문 체크리스트는 한국어로 작성한다.
- 코드명, 제품명과 실제 파일 경로는 정확성을 위해 원래 표기를 유지한다.
- 구현 및 검증이 끝나기 전에는 진행 중 항목을 완료로 체크하지 않는다.

---

# 요청 시작: 2026-08-06 01:02:13 KST | 작업 완료: 2026-08-06 01:02:47 KST (소요 시간: 34초)

## 구현 내역

- `CANDIDATE_FEATURES.md`의 Interchange 절에 있는 모든 후보를 제외로 표시했다.
- BMP/TIFF/WebP, GIF/APNG, OpenEXR, SVG, PDF, Camera RAW, EXIF/XMP metadata, batch export, Windows 파일 연결 및 탐색기 thumbnail 후보에 `[x] 제외` 결정을 기록했다.
- 검토 결과 반영란에 Interchange 전체 제외 결정을 기록했다.

## 결정 내용

- 이번 제외 결정은 후보 기능 문서의 Interchange 절에만 적용한다.
- 기존에 확정된 `.ocp`, PNG, JPEG, PSD 읽기/쓰기 범위는 후보가 아니므로 계획을 그대로 유지한다.

---

# 요청 시작: 2026-08-06 00:52:30 KST | 작업 완료: 2026-08-06 01:04:32 KST (소요 시간: 12분 2초)

## 구현 내역

- Core `LayerProperties`에 기본값이 해제 상태인 `alpha_locked` 속성을 추가했다.
- 모든 레이어에서 투명도 잠금을 변경할 수 있는 `SetAlphaLocked` API를 구현했다.
- 레이어 속성 테스트에 기본 상태와 활성화 상태 검증을 추가했다.

## 결정 내용

- 일반 잠금과 투명도 잠금은 서로 독립적인 레이어 속성으로 유지한다.
- 투명도 잠금의 실제 페인팅 규칙과 Application 명령 연결은 각각 도구 엔진 및 문서 명령 단위에서 별도 검증한다.

---

# 요청 시작: 2026-08-06 00:36:39 KST | 작업 완료: 2026-08-06 01:05:39 KST (소요 시간: 29분)

## 구현 내역

- 7종 `ToolKind`와 UI 비종속 좌표, 색상, 스타일러스 입력 계약을 구현했다.
- Pencil의 빠른 이동 구간을 Bresenham 정수 픽셀로 빈틈없이 연결하고 앤티앨리어싱 coverage를 생성하지 않도록 구현했다.
- 크기, Hardness, Spacing, Flow, Opacity와 압력의 크기·Opacity 영향 토글을 갖는 Airbrush/Brush 설정을 구현했다.
- 고정 시간과 거리 간격 후보를 결정적으로 병합하고 입력 이벤트 분할과 무관하게 같은 Dab 경로를 생성하도록 구현했다.
- Hardness 기반 중심-가장자리 Opacity 감쇠와 premultiplied source-over 타일 합성을 구현했다.
- 투명도 잠금 시 완전 투명 대상 픽셀을 건너뛰고 기존 Alpha를 보존한 채 색상만 합성하도록 구현했다.
- 사각형·타원형 Marquee와 자유형·다각형 Lasso의 이진 선택 마스크를 구현했다.
- Gamma 기반 압력 감도 매핑과 결정적 Stroke Stabilizer, 종료점 보장 `Flush`를 구현했다.
- 전경색·배경색 교체와 검정/흰색 초기화 상태를 구현했다.
- 도구 전용 테스트 프로젝트를 추가해 선 연결성, 브러시 동역학, 간격 결정성, 압력 토글, Hardness, Alpha Lock, 선택 경계, 감도와 안정화를 검증했다.

## 결정 내용

- 브러시 Spacing은 압력 반영 후 Dab 지름에 대한 배수로 정의한다.
- Hardness는 기하 coverage를 이진 값으로 유지하면서 Dab Opacity 감쇠에만 반영한다.
- 스타일러스 압력은 크기와 Opacity에 독립적으로 적용 여부를 선택할 수 있다.
- Stroke Stabilizer는 플랫폼 입력 형식에 의존하지 않고 `StylusSample` 스트림을 소비하며 마지막 원시 입력점을 정확한 종점으로 확정한다.
- 모든 도구 결과는 WinUI, WinRT, Win32, Direct3D 형식을 공개 API에 노출하지 않는다.

---

# 요청 시작: 2026-08-06 00:47:10 KST | 작업 완료: 2026-08-06 01:06:48 KST (소요 시간: 19분 38초)

## 구현 내역

- 각 문서가 기본 Raster Layer와 활성 레이어를 가지도록 Application 작업 공간을 Core `LayerTree`에 연결했다.
- 문서와 레이어를 닫아도 재사용되지 않는 Application `LayerId`를 구현했다.
- 부모 ID, 깊이, 형제 순서, 종류와 공통 속성을 제공하는 UI 중립 flat pre-order `LayerSummary`를 구현했다.
- Raster/Group 추가, 제거, 이동, 이름, Opacity, 가시성, 투명도 잠금과 Blend Mode를 변경하는 9개 undoable 명령을 구현했다.
- 제거한 하위 트리의 정확한 복구, 이동 전 위치 복구와 활성 레이어 복구를 구현했다.
- 레이어 변경을 기존 문서별 history, saved revision 및 dirty 상태 판정에 연결했다.
- 전용 Application 레이어 테스트를 추가해 문서 격리, 중첩 순서, 모든 속성의 Undo/Redo, 저장 상태와 ID 비재사용을 검증했다.

## 결정 내용

- WinUI에는 Core 객체를 직접 노출하지 않고 분리된 Application 스냅샷만 제공한다.
- 레이어 명령 하나는 문서 history의 revision 하나를 차지하며 실패 시 부분 변경을 남기지 않는다.
- 투명도 잠금은 일반 잠금 및 Opacity와 독립된 속성으로 스냅샷과 Undo/Redo에 포함한다.
- 레이어 ID는 문서 사이에서도 고유하며 닫힌 문서의 ID를 새 레이어에 재사용하지 않는다.

---

# 요청 시작: 2026-08-06 01:03:18 KST | 작업 완료: 2026-08-06 01:08:29 KST (소요 시간: 5분 11초)

## 구현 내역

- 레이어 후보 가운데 Merge Down·Merge Visible·Flatten, 복수 레이어 선택과 일괄 속성, Text Layer, Fill Layer, Layer Effects, 색상 태그·검색·필터, Linked/Smart Object를 채택 상태로 기록했다.
- `Vector Shape Layer`는 제외 상태로 기록했다.
- 채택한 기능과 완료 조건을 제품 요구사항 문서로 승격했다.
- UI 중립 도메인 타입, 원자적 명령, Render Graph, Cache, `.ocp` 저장과 PSD 호환 규칙을 편집기 아키텍처 문서에 설계했다.
- 채택 기능 7개를 한국어 진행 체크리스트의 미착수 항목으로 추가했다.
- 현재 완료된 도구 엔진, 투명도 잠금과 Application 레이어 명령 상태도 진행 체크리스트에 반영했다.

## 결정 내용

- `Vector Shape Layer` 타입, 명령, 편집 UI 및 저장 계약은 제품 범위에서 제외한다.
- Text Layer와 Fill Layer 채택은 Vector Shape 편집이나 SVG 지원을 의미하지 않는다.
- 채택한 기능은 설계 범위로 확정하지만 이번 문서 작업에서 구현하지 않는다.
- 복수 레이어 변경과 병합은 부분 적용 없이 하나의 원자적 Undo 명령으로 처리한다.
- Linked/Smart Object의 외부 원본이 누락돼도 마지막 정상 Cache를 유지하고 명시적 진단을 제공한다.

---

# 요청 시작: 2026-08-06 01:10:43 KST | 작업 완료: 2026-08-06 01:16:00 KST (소요 시간: 0시간 5분 17초)

## 구현 내역

- `CANDIDATE_FEATURES.md`의 Essential workflow 후보 7개를 모두 채택 상태로 변경했다.
- Painting 후보 가운데 일반 Paint Brush, Eraser, Eyedropper, Paint Bucket, Gradient, Brush Preset, 대칭·미러 페인팅, Clone Stamp 및 Healing을 채택했다.
- `Line 및 기본 Shape 도구`는 보류 상태로 기록하고 제품 요구사항의 도구 ID, 명령 및 도메인 타입에는 포함하지 않았다.
- 채택 기능의 동작과 완료 기준을 제품 요구사항에 추가했다.
- 명령 레지스트리, 설정 포트, 클립보드, 복제, 이력 스냅샷 및 페인팅 도메인의 UI 중립 경계를 편집기 아키텍처에 설계했다.
- 채택한 기능을 `PROGRESS.md`에 미착수 체크 항목으로 추가했다.

## 결정 내용

- Essential workflow의 모든 후보는 구현 대상 설계 범위로 확정한다.
- Painting은 Line 및 기본 Shape만 보류하며 나머지 후보는 구현 대상 설계 범위로 확정한다.
- 보류 기능은 래스터화, stroke/fill 및 안티앨리어싱 정책을 별도로 확정할 때 재검토한다.
- 이번 작업은 설계 승격만 수행하며 새로 채택한 기능의 구현 완료를 의미하지 않는다.
- 기존 Layers 채택, Vector Shape Layer 제외 및 Interchange 전체 제외 결정은 그대로 유지한다.

---

# 요청 시작: 2026-08-06 01:08:43 KST | 작업 완료: 2026-08-06 01:17:28 KST (소요 시간: 0시간 8분 45초)

## 구현 내역

- 활성 도구, 전경색·배경색, Brush, 압력 감도 및 Stroke Stabilizer 설정을 보관하는 UI 중립 Application `EditorState`를 구현했다.
- 설정값의 일괄 변경과 개별 변경, 색 교체·초기화, 변경 revision 및 동일 값 변경 억제를 구현했다.
- WinUI와 Core 타입을 공개 API에서 분리하고 Core 도구 타입과 검증기의 매핑을 구현 파일 내부로 격리했다.
- 유효하지 않은 입력은 상태와 revision을 바꾸기 전에 거부하고, 이전 스냅샷이 이후 변경의 영향을 받지 않도록 값 기반 스냅샷을 제공했다.
- 전용 테스트 프로젝트를 솔루션에 추가하고 Debug x64 빌드 및 테스트를 통과했다.

## 결정 내용

- 편집 도구 설정은 문서 내용이 아니므로 문서별 Undo/Redo, 저장 revision 및 dirty 상태와 분리한다.
- 프런트엔드는 Application 스냅샷을 투영하고 설정 변경 API만 호출하도록 하여 WinUI를 다른 UI로 교체할 수 있게 한다.
- 입력 검증은 Core의 도구 계약을 기준으로 하되 Core 타입은 Application 공개 경계를 넘기지 않는다.

---

# 요청 시작: 2026-08-06 00:36:39 KST | 작업 완료: 2026-08-06 01:20:09 KST (소요 시간: 0시간 43분 30초)

## 구현 내역

- WinUI 편집기 셸에 상호 배타적인 7종 도구를 선택하는 왼쪽 세로 툴바를 구현했다.
- 툴바 하단에 대각선으로 겹친 전경색·배경색 견본과 색 교체·검정/흰색 초기화 동작을 구현했다.
- 각 색 견본에서 HSV와 Alpha, RGB 및 16진수 값을 동기화해 편집하는 색상 선택 Flyout을 구현했다.
- 메뉴바 아래에 Brush Size, Hardness, Spacing, Flow, Opacity와 압력의 Dab Size·Opacity 영향 토글을 제공하는 Tool Options Bar를 구현했다.
- 스타일러스가 캔버스에서 감지되면 압력 옵션을 활성화하고 현재 압력 상태를 표시하도록 구현했다.
- Stroke Stabilizer 토글과 Pressure Gamma, 입력 범위, Stabilizer 강도·Smoothing을 조절하는 감도 설정 대화상자를 구현했다.
- 문서 탭과 기본 메뉴바를 편집기 셸에 추가하고 전체 Debug x64 솔루션 빌드와 6개 headless 테스트를 통과했다.

## 결정 내용

- 이 커밋에는 편집기 셸만 포함하고 별도 작업인 스플래시, 저장소 링크, 앱 아이콘, 이미지 자산 및 설치 프로그램 변경은 포함하지 않는다.
- 툴바는 도구 상태를 투영하며 도구 알고리즘과 문서 변경 규칙은 Core 및 Application에 둔다.
- 색상 선택기의 내부 기준은 HSV와 RGBA이며 16진수 입력은 같은 값을 표현하는 편집 형식으로 취급한다.
- 스타일러스 압력의 Dab Size와 Opacity 영향은 서로 독립적으로 켜고 끌 수 있다.

---

# 요청 시작: 2026-08-06 01:15:06 KST | 작업 완료: 2026-08-06 01:21:12 KST (소요 시간: 0시간 6분 6초)

## 구현 내역

- `build-release.bat`의 Release 검증 단계에 `OctoPaint.Tools.Tests` 실행을 추가했다.
- `OctoPaint.Application.Layer.Tests`와 `OctoPaint.Application.EditorState.Tests` 실행을 추가했다.
- 각 테스트 실행 파일이 생성됐는지 확인하고 누락 또는 실패 시 릴리스 생성을 중단하도록 했다.
- 전체 Debug x64 솔루션 빌드와 6개 headless 테스트를 실행해 현재 통합 상태를 검증했다.

## 결정 내용

- ZIP 및 MSI는 Core, Application, 도메인, 도구, 레이어 명령과 편집 상태 테스트가 모두 통과한 경우에만 생성한다.
- 각 테스트 실패 메시지에는 실패한 프로젝트 이름을 명시한다.
- 기존 6단계 릴리스 흐름과 버전 포함 ZIP/MSI 파일명 규칙은 유지한다.

---

# 요청 시작: 2026-08-06 01:22:04 KST | 작업 완료: 2026-08-06 01:30:37 KST (소요 시간: 0시간 8분 33초)

## 구현 내역

- WinUI의 임시 `ToolOptionsState`를 제거하고 Application의 UI 중립 `EditorState`를 편집 상태의 단일 원천으로 연결했다.
- 7종 도구 선택, 전경색·배경색 교체·초기화·편집, Brush 옵션, 압력 토글 및 Stroke Stabilizer 변경이 Application API를 거치도록 구현했다.
- Application 스냅샷에서 툴바 선택 상태, 색상 견본, Tool Options Bar와 감도 설정 대화상자를 다시 투영하도록 구현했다.
- 압력 프리셋의 중복 로컬 상태를 제거하고 Linear, Soft, Firm 선택을 Gamma 1.0, 0.5, 2.0으로 매핑하며 그 외 값은 Custom으로 표시하도록 했다.
- 사용자 소유 스플래시·아이콘 변경이 없는 격리 복제본에서 의존성 복원, 전체 Debug x64 빌드 및 6개 headless 테스트를 통과했다.

## 결정 내용

- WinUI는 편집 상태를 별도로 보관하지 않고 Application 스냅샷을 투영하는 교체 가능한 어댑터로 유지한다.
- 도구 설정 변경은 문서 픽셀 변경이 아니므로 문서 Undo/Redo와 dirty 상태를 만들지 않는다.
- 색상 선택 Flyout의 취소는 Application 상태를 변경하지 않으며 적용 시에만 해당 색을 갱신한다.
- 별도 진행 중인 스플래시, 아이콘, 이미지 자산 및 설치 프로그램 변경은 이번 커밋에 포함하지 않는다.

---

# 요청 시작: 2026-08-06 01:21:18 KST | 작업 완료: 2026-08-06 01:23:54 KST (소요 시간: 0시간 2분 36초)

## 구현 내역

- 기존 캐릭터 시안을 수정하거나 참조하지 않고 은발 양갈래 소녀 메이드 스플래시 원본을 ImageGen으로 새로 생성했다.
- 검은 상의와 같은 재질의 검은 치마, 흰 앞치마, 앞치마 왼쪽·오른쪽 아래 끝의 작은 검은 리본 두 개를 반영했다.
- 캐릭터와 구분되는 저채도 더스티 청록 단색 배경을 적용했다.
- 생성 원본을 `artwork/imagegen-sources/2026-08-06/20-splash-character-girl-muted-solid-background-new-source.png`에 저장하고 이미지 에셋 카탈로그에 목적과 상태를 기록했다.

## 결정 내용

- 이 파일은 사용자가 직접 후가공할 원본이므로 알파 추출, 크로마키 제거, 리사이즈, 합성 또는 색상 보정을 적용하지 않는다.
- 기존 ImageGen 원본과 파생 에셋을 덮어쓰지 않고 새 번호의 원본으로 보존한다.
- 앱 빌드에는 원본 아카이브를 포함하지 않으며, 사용자가 후가공을 마친 뒤 최종 `Assets` 경로에 별도로 반영한다.

---

# 요청 시작: 2026-08-06 00:41:52 KST | 작업 완료: 2026-08-06 01:34:14 KST (소요 시간: 0시간 52분 22초)

## 구현 내역

- ImageGen으로 민트색 문어가 프랑스식 베레모를 쓰고 붓을 든 앱 아이콘을 새로 생성하고 WinUI 실행 파일과 MSI 바로가기에 적용했다.
- 편집기의 7개 도구 아이콘을 순수 무채색 그레이스케일 PNG로 생성하고 기존 문자 글리프를 이미지로 교체했다.
- 은발 양갈래 메이드 캐릭터와 무지개 솜사탕 구름 배경을 분리해 스플래시 레이어로 적용하고, 약한 시차·카메라 이동 효과와 모션 축소 정적 폴백을 추가했다.
- 스플래시에 이미지가 아닌 `by octopus7` 텍스트와 GitHub 리포지터리 링크 버튼을 추가했다.
- 모든 ImageGen 출력 원본을 `artwork/imagegen-sources/2026-08-06/`에 덮어쓰기 없이 보존하고, `docs/GENERATED_IMAGE_ASSETS.md`에 생성 목적·상태·사용 경로를 기록했다.
- 전체 Release x64 솔루션 빌드와 Core·Application·Layer·EditorState·Tools 등 6개 headless 테스트를 통과했다.

## 결정 내용

- 앱 아이콘은 은은한 민트색 문어를 유지하고, 편집 도구 아이콘은 메인 UI의 퍼플 테마와 분리된 중립 무채색만 사용한다.
- 설계 문서는 이미지를 임의로 재생성하지 않고 자산 카탈로그에 기록된 파일을 꺼내 쓰도록 명시한다.
- 스플래시 캐릭터의 후가공 품질을 위해 생성 원본을 반드시 보존하고, 사용자가 요청한 신규 단색 배경 원본에는 투명화·크로마키 제거·리사이즈·합성을 적용하지 않는다.
- 새로 생성한 후가공용 원본은 현재 실행 스플래시에 자동 교체하지 않고, 사용자가 분리 작업을 마친 뒤 최종 자산으로 반영한다.

---

# 요청 시작: 2026-08-06 01:39:49 KST | 작업 완료: 2026-08-06 01:40:15 KST (소요 시간: 0시간 0분 26초)

## 구현 내역

- 미추적 상태였던 `src/OctoPaint.WinUI/ToolOptionsState.h`를 저장소 추적 대상으로 포함했다.
- 현재 `MainWindow`와 WinUI 프로젝트 파일이 참조하는 Brush, 압력 및 Stroke Stabilizer UI 상태 정의를 깨끗한 clone에서도 사용할 수 있게 했다.

## 결정 내용

- 최신 WinUI 구현이 직접 참조하는 `ToolOptionsState.h`를 누락 파일로 남기지 않고 현재 소스 구성에 포함한다.
- 이번 변경은 누락 파일 포함만 수행하며 편집 상태 아키텍처를 다시 변경하지 않는다.

---

# 요청 시작: 2026-08-06 15:42:57 KST | 작업 완료: 2026-08-06 15:53:39 KST (소요 시간: 0시간 10분 42초)

## 구현 내역

- UI 중립 `PaintStrokeRequest`와 Pencil/Airbrush 스트로크 적용 API를 Application에 추가했다.
- Core의 Pencil, Airbrush, 압력 감도 및 Stroke Stabilizer 엔진을 활성 Raster Layer의 희소 타일에 연결했다.
- 스트로크 하나를 문서 이력 하나로 적용하고 정확한 Undo/Redo 타일 상태를 보존하도록 구현했다.
- 문서 경계 clipping, 일반 잠금, 투명도 잠금, 대상 문서·레이어 및 입력 유효성 검사를 추가했다.
- 렌더러가 사용할 canvas-sized premultiplied BGRA8 Raster 스냅샷을 UI 비종속 값으로 제공했다.
- 전용 Application Paint 테스트를 솔루션과 Release 테스트 게이트에 추가하고 Debug/Release x64에서 통과시켰다.

## 결정 내용

- 한 번의 포인터 제스처는 하나의 Application 페인트 요청과 하나의 Undo 항목으로 확정한다.
- 실패하거나 실제 픽셀이 변하지 않은 요청은 revision, history 및 타일 상태를 변경하지 않는다.
- WinUI와 D3D 형식은 Application 공개 API에 노출하지 않고 BGRA 바이트 스냅샷만 전달한다.
- 이번 커밋은 실제 캔버스를 위한 Application 페인팅 경계를 완결하며 WinUI 입력과 D3D 렌더링 연결은 후속 독립 커밋으로 분리한다.

---

# 요청 시작: 2026-08-06 15:42:57 KST | 작업 완료: 2026-08-06 15:56:47 KST (소요 시간: 0시간 13분 50초)

## 구현 내역

- WinUI `SwapChainPanel`에 연결할 수 있는 Direct3D 11 및 Direct2D 캔버스 렌더러를 추가했다.
- 하드웨어 Direct3D 장치 생성 실패 시 WARP 장치로 전환하고, 장치 손실 시 렌더링 자원을 다시 생성하도록 구현했다.
- premultiplied BGRA8 문서 스냅샷 표시, 투명 체크무늬 배경, 화면 중앙 맞춤 확대·축소와 패널 좌표의 문서 픽셀 좌표 변환을 구현했다.
- DPI 및 패널 크기 변경에 맞춰 스왑 체인을 재설정하고 원본 스냅샷을 소유 복사하여 호출자 수명과 분리했다.

## 결정 내용

- 렌더러는 Core 또는 Application 타입에 의존하지 않는 WinUI 어댑터로 유지하고, 바이트 뷰만 입력받도록 경계를 확정했다.
- GPU 하드웨어 경로를 우선 사용하되 그래픽 장치가 제한된 환경에서도 동작하도록 WARP 폴백을 채택했다.
- UI 포인터 수집과 Application 스트로크 확정은 별도 후속 커밋으로 분리한다.

---

# 요청 시작: 2026-08-06 15:42:57 KST | 작업 완료: 2026-08-06 15:59:53 KST (소요 시간: 0시간 16분 56초)

## 구현 내역

- 새 문서의 검은 자리표시자 영역을 실제 `SwapChainPanel` 기반 캔버스로 교체하고 활성 Raster Layer의 BGRA 스냅샷을 표시했다.
- Pencil과 Airbrush에서 포인터 누름·이동·놓기, 취소 및 캡처 손실을 처리하고 한 제스처를 한 Application 스트로크로 확정했다.
- 마우스와 스타일러스의 중간 포인터 지점을 시간순으로 수집하고, 스타일러스 압력과 Tool Options의 크기·경도·간격·Flow·Opacity·압력·Stabilizer 값을 스트로크 요청에 반영했다.
- 문서 또는 레이어 전환 뒤 활성 레이어 스냅샷을 다시 표시하고, 캔버스 크기와 DPI 변경을 D3D 렌더러에 전달했다.
- 임시 `ToolOptionsState`를 제거하고 WinUI 편집 상태를 Application `EditorState`로 다시 통합했다.

## 결정 내용

- 포인터 이동 중에는 샘플만 수집하고 놓는 시점에 스트로크를 한 번 확정하여 제스처당 단일 Undo 항목을 유지한다.
- 캔버스 밖에서 시작한 입력은 무시하고, 캡처 뒤 캔버스를 벗어난 지점은 문서 좌표로 매핑되는 샘플만 보존한다.
- 현재 직접 그리기는 구현된 Pencil과 Airbrush에 한정하며 선택·이동 도구는 각 전용 명령 경로가 연결될 때까지 캔버스를 변경하지 않는다.

---

# 요청 시작: 2026-08-06 15:43:49 KST | 작업 완료: 2026-08-06 16:12:56 KST (소요 시간: 0시간 29분 7초)

## 구현 내역

- Tools와 Layers 패널을 메인 창의 도킹 호스트와 별도 WinUI 부유 창 사이에서 전환하는 공통 `DockablePanelController`를 추가했다.
- 각 패널의 Float/Dock 버튼, 열 너비 접기·복원, 부유 창 크기와 접근성 이름 갱신을 구현했다.
- 부유 창의 닫기 버튼을 누르면 종료를 취소하고 패널을 원래 MainWindow 위치로 자동 재도킹하도록 구현했다.
- 패널 이동을 routed event가 끝난 뒤 DispatcherQueue에서 수행하고, Float 도중 실패하면 원래 호스트로 되돌리는 복구 경로를 추가했다.
- MainWindow 종료 시 이벤트 해제, 콘텐츠 분리, 부유 창 종료 순서로 안전하게 정리하고 패널 전환 전에 열린 색상 편집 Flyout을 닫도록 했다.

## 결정 내용

- 상단 Menu, Tool Options, CommandBar와 하단 Status 영역은 MainWindow에 고정한다.
- 현재 분리 대상은 사이드 Tools/Layers 패널로 확정하고 중앙 문서 캔버스는 입력·렌더링 수명을 유지하기 위해 고정한다.
- 추가 패널은 MainWindow 전용 분기 대신 descriptor 등록으로 동일한 도킹·부유 구조에 참여하도록 설계한다.
- 모든 부유 창은 MainWindow와 같은 UI thread에서 동작하며 Application `Workspace`와 `EditorState`를 공유한다.

---

# 요청 시작: 2026-08-06 15:42:57 KST | 작업 완료: 2026-08-06 16:23:54 KST (소요 시간: 0시간 40분 57초)

## 구현 내역

- 한 페인트 요청 안에 서로 연결되지 않는 복수 스트로크 구간을 표현하는 `begins_new_segment` 샘플 경계를 추가했다.
- 캔버스를 벗어났다가 재진입한 포인터 경로를 새 구간으로 시작하여 문서를 가로지르는 의도하지 않은 직선을 방지했다.
- 구간마다 Stabilizer와 Airbrush 누적·간격 상태를 초기화하면서 전체 제스처는 하나의 Application 요청과 하나의 Undo 항목으로 유지했다.
- 포인터 누름 시 Tool, 전경색, Brush, 압력 및 Stabilizer 설정을 고정하고 스트로크 완료까지 같은 설정을 사용하도록 변경했다.
- Release와 Cancel의 포인터 캡처를 best-effort로 먼저 해제하고, Capture Lost는 마지막 유효 샘플까지 확정하도록 수명 경계를 보강했다.
- D3D 전송 전 Raster snapshot stride가 32비트 pitch 범위 안인지 검사하고 Pencil/Airbrush 구간 단절 테스트를 추가했다.

## 결정 내용

- 문서 밖 이동은 픽셀을 만들지 않되 다음 유효 샘플을 새 구간의 시작으로 표시한다.
- Cancel은 누적 입력을 폐기하고, 운영체제 또는 창 상태로 인한 Capture Lost는 사용자가 이미 그린 유효 구간을 보존한다.
- 스트로크 도중 UI 설정 변경은 다음 스트로크부터 적용하며 진행 중 요청의 의미는 바꾸지 않는다.
- Group Layer와 전체 문서 합성 스냅샷 분리는 실제 Layers 기능 구현 시 별도 렌더링 경계로 다룬다.

---

# 요청 시작: 2026-08-06 15:43:49 KST | 작업 완료: 2026-08-06 16:24:57 KST (소요 시간: 0시간 41분 8초)

## 구현 내역

- 패널별 전환 대기 상태를 추가하여 빠른 연속 Float/Dock 클릭이 반대 전환을 연달아 실행하지 않도록 했다.
- 지연 전환, 부유 창 Closing 및 Closed callback이 컨트롤러 수명을 weak token으로 확인한 뒤에만 상태에 접근하도록 보강했다.
- 사용자 닫기 재도킹을 큐에 넣지 못하면 닫기 취소를 해제하여 창이 닫히지도 도킹되지도 않는 상태를 방지했다.
- Dock 중 XAML 재부모화 또는 Window Close 실패 시 부유 상태로 복원하고 예외가 UI callback 밖으로 전파되지 않도록 했다.
- MainWindow 종료 시 이벤트 해제, 콘텐츠 분리와 창 닫기를 각각 독립적인 best-effort 단계로 수행하도록 정리했다.

## 결정 내용

- MainWindow는 직접 Toggle을 큐에 넣지 않고 컨트롤러의 `RequestToggle`에 요청하여 전환 직렬화와 수명 정책을 한곳에서 관리한다.
- 내부 Window Close와 사용자 Close를 구분하여 내부 정리 중 Closing/Closed 재진입이 상태를 다시 변경하지 않게 한다.
- 종료 경로에서는 일부 WinUI 호출이 실패해도 남은 이벤트와 창 정리를 계속 수행하는 것을 우선한다.

---

# 요청 시작: 2026-08-06 17:25:36 KST | 작업 완료: 2026-08-06 17:36:48 KST (소요 시간: 0시간 11분 12초)

## 구현 내역

- Debug 실행 파일의 즉시 종료를 재현하고 종료 코드 `0xC000027B`, Windows Error Reporting 내부 HRESULT `0x80004002 (E_NOINTERFACE)`를 확인했다.
- `IDXGIAdapter`에서 factory를 QueryInterface로 요청하던 코드를 `IDXGIAdapter::GetParent` 기반 `IDXGIFactory2` 취득으로 수정했다.
- 필수 렌더링 경로가 아닌 `IDXGISwapChain2` 변환과 `SetMaximumFrameLatency` 호출을 제거하고 기본 `IDXGISwapChain1`을 유지하도록 변경했다.
- WinUI 버전에 따라 `ISwapChainPanelNative2` 또는 `ISwapChainPanelNative`를 선택하도록 SwapChainPanel 네이티브 연결 경로를 보강했다.
- 전체 Debug x64 빌드와 7개 headless 테스트를 통과시키고, 수정된 실행 파일이 5초 이상 생존하는 것을 직접 확인했다.

## 결정 내용

- DXGI adapter와 factory는 서로 QueryInterface 관계가 아니므로 부모 객체 조회 API를 사용한다.
- `IDXGISwapChain2`의 선택적 프레임 지연 기능은 지원되지 않는 Windows/DXGI 조합에서 앱 시작을 중단시키지 않도록 필수 경로에서 제외한다.
- SwapChainPanel 네이티브 인터페이스를 제공하지 않는 환경에서는 프로세스를 종료시키지 않고 렌더 연결만 건너뛴다.

---

# 요청 시작: 2026-08-06 17:39:45 KST | 작업 완료: 2026-08-06 17:42:39 KST (소요 시간: 0시간 2분 54초)

## 구현 내역

- 새 문서의 최초 기본 Raster Layer 전체를 premultiplied RGBA8 불투명 흰색 `(255, 255, 255, 255)` 타일로 초기화했다.
- 하나의 immutable 흰색 TilePayload를 전체 타일 항목이 공유하고, 실제 페인팅이 발생한 타일만 Copy-on-Write draft로 분리되도록 구성했다.
- 새 문서 Raster snapshot의 모든 픽셀이 불투명 흰색인지 전체 순회하는 테스트를 추가했다.
- 흰색 초기 상태에 맞게 Pencil, Airbrush, 구간 단절, Undo 및 detached snapshot 테스트 기대값을 갱신했다.
- 투명도 잠금 테스트는 새로 추가한 투명 Raster Layer에서 수행하여 추가 레이어의 투명 기본값을 계속 검증했다.

## 결정 내용

- 렌더러 배경만 흰색으로 바꾸지 않고 실제 문서 픽셀을 흰색으로 저장하여 편집, Alpha Lock 및 향후 파일 저장 결과가 화면과 일치하게 한다.
- 흰색 초기화는 `Workspace::NewDocument`가 만드는 최초 레이어에만 적용하고 이후 추가 Raster Layer는 투명 상태를 유지한다.
- 기존 문서 로딩과 문서 포맷 경로에는 이번 기본값 변경을 적용하지 않는다.
